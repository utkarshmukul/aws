events {}

http {

    upstream car_api {
        least_conn;
        
        server car_service1:8080;
        server car_service2:8080;
        server car_service3:8080;
    }

    server {
        listen 80;

        location /cars {
            proxy_pass http://car_api;
        }
    }
}